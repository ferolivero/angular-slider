export enum TipoSlider {
  Fixed = 'fixed',
  Normal = 'normal'
}

export enum TipoPunto {
  Min,
  Max
}

export enum TipoLabel {
  ValorInferior,
  ValorSuperior,
  LimiteInferior,
  LimiteSuperior
}
